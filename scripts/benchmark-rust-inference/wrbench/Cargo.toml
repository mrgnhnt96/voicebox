[package]
name = "wrbench"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone benchmark, not part of the app workspace.
[workspace]

[dependencies]
whisper-rs = { version = "0.16", features = ["metal"] }
hound = "3"
libc = "0.2"
serde_json = "1"

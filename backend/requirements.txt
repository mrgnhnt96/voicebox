# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Database
sqlalchemy>=2.0.0

# ML models (Whisper speech-to-text and the Qwen3 cleanup LLM).
# PyTorch is the fallback backend when MLX is unavailable.
torch>=2.2.0
transformers>=4.36.0,<=4.57.6
huggingface_hub>=0.20.0

# Audio processing
audioop-lts>=0.2.1; python_version >= "3.13"
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0,<2.0
numba>=0.60.0,<0.61.0

# HTTP client (used by FastAPI's TestClient in the test suite)
httpx>=0.27.0

# Utilities
python-multipart>=0.0.6
